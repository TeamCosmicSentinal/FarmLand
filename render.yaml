services:
  - type: web
    name: agriguru-backend
    env: python
    plan: free
    buildCommand: |
      python -m pip install --upgrade pip setuptools wheel
      pip install -r backend/requirements.txt
    startCommand: gunicorn -w 2 -k gthread --threads 4 --timeout 120 -b 0.0.0.0:$PORT backend.wsgi:application
    healthCheckPath: /health
    autoDeploy: true
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9
      - key: PYTHONPATH
        value: backend
      - key: GEMINI_API_KEY
        sync: false
      - key: OPENWEATHER_API_KEY
        sync: false
      - key: MONGO_URI
        sync: false
      - key: MONGO_DB
        sync: false
      - key: JWT_SECRET
        sync: false
      - key: ADMIN_SECRET
        sync: false
      # Set this to your frontend public URL later (e.g., https://agriguru-frontend.onrender.com)
      - key: FRONTEND_ORIGIN
        value: "*"

  - type: web
    name: agriguru-frontend
    env: node
    plan: free
    buildCommand: cd frontend && npm ci && npm run build
    startCommand: cd frontend && npx serve -s build -l $PORT
    autoDeploy: true
    envVars:
      # Set this to your backend public URL later (e.g., https://agriguru-backend.onrender.com)
      - key: REACT_APP_API_BASE
        sync: false