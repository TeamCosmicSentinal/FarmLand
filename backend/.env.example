# Copy to .env for local development. On Render, set these as environment variables in the dashboard.
GEMINI_API_KEY=
OPENWEATHER_API_KEY=
MONGO_URI=
MONGO_DB=agriguru
# Set the deployed frontend origin for CORS, e.g. https://agriguru-frontend.onrender.com
FRONTEND_ORIGIN=
